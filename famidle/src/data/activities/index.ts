import type { ActivityType } from '@/types/ActivityType'
import { age1Activities } from './age1'

/** Données concaténées pour `activityStore`. */
export const activitiesData: ActivityType[] = [...age1Activities]
